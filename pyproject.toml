[tool.poetry]
name = "toolkit"
version = "0.1.0"
description = ""
authors = ["octavedbc <octave.dbc@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
streamlit = "^1.26.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
